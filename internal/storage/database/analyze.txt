CRITICAL BUGS FOUND:

1. postgres_orgs.go:230 - ListKnowledgeDocuments MISSING rows.Err()
   - The function iterates with rows.Next() but never checks rows.Err()
   - This means iteration errors are silently ignored
   - Should be: return docs, rows.Err()

2. postgres_orgs.go:304 - SearchKnowledge MISSING rows.Err()
   - Same issue: iterates with rows.Next() but doesn't check rows.Err()
   - Should add: if err := rows.Err(); err != nil { return nil, err }

3. postgres_orgs.go:279 - json.Unmarshal error ignored
   - Line 279: json.Unmarshal(embJSON, &c.Embedding)
   - The error is ignored with no error check
   - Should be: if err := json.Unmarshal(...); err != nil { ... }

4. postgres_flows.go:260 - SearchKnowledge identical bug
   - Same rows.Err() missing after rows iteration

Let me verify these exact lines...
